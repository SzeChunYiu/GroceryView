/goal You are PANE 0, lane PLATFORM. Read docs/parallel-sessions.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 1, lane INTEGRATION. Read docs/parallel-sessions.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 2, lane PLATFORM. Read docs/parallel-sessions.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 3, lane INTEGRATION. Read docs/parallel-sessions.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 4, lane PLATFORM. Read docs/parallel-sessions.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
