{
  "name": "groceryview",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @groceryview/core && npm run build -w @groceryview/api && npm run build -w @groceryview/web",
    "test": "npm run test -w @groceryview/core && npm run test -w @groceryview/api",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  }
}
