generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum AdminRole {
  ADMIN
  SUPER_ADMIN
}

model AppUser {
  id       String    @id
  email    String?   @unique
  role     AdminRole @default(ADMIN)
}

