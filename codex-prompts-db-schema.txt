/goal You are PANE 0, lane DB-SCHEMA. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 1, lane DB-SCHEMA. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 2, lane DB-SCHEMA. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 3, lane INFRA. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 4, lane INFRA. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
