export const listsRoutes = {
  controllerPath: 'lists',
  shareAction: 'share-url',
  webListPath: '/list',
  shareParam: 'share',
  shareEncodingVersion: 1,
  maxShareItems: 100,
  description: 'Shopping list share URLs encode item id, name, quantity, and detail in the URL so recipients can open a list without an account.'
} as const;
