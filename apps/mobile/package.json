{
  "name": "@groceryview/mobile",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "npm run build -w @groceryview/core && npm run build -w @groceryview/monetization && npm run build -w @groceryview/api && tsc -p tsconfig.test.json && node --test dist-test/__tests__/*.test.js"
  },
  "dependencies": {
    "@groceryview/api": "0.1.0",
    "expo": "^56.0.3",
    "expo-camera": "^56.0.7",
    "expo-router": "^56.2.5",
    "react": "^19.2.6",
    "react-native": "^0.85.3"
  }
}
