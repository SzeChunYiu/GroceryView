# Playwright fixtures

Import `test` and `expect` from `./fixtures/base` for browser tests. The shared config runs chromium, firefox, and webkit with junit/html reporters plus screenshot and trace retention on failure.
