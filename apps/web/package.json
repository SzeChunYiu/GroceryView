{
  "name": "@groceryview/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm run build -w @groceryview/core && tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "test": "node --test scripts/*.test.mjs"
  },
  "dependencies": {
    "@groceryview/core": "0.1.0"
  }
}
