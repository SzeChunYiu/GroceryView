export const friendPriceActivityPrivacyContext = {
  anonymizationLabel: 'Anonymized friend signal',
  consentLabel: 'Only opt-in sightings shared with friends are shown',
  locationContextLabel: 'Location is rounded to store or neighborhood context; no account address is exposed.'
} as const;
