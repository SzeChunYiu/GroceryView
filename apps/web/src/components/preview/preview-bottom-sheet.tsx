'use client';

/** Mobile-first bottom sheet — same responsive overlay as PreviewDrawer (sheet on small screens, side panel on md+). */
export { PreviewDrawer as PreviewBottomSheet } from './preview-drawer';
