# Art Credits — apps/web/public/art/

## Hero art (GeometricHeroArt component)

All art in `apps/web/src/components/home/geometric-hero-art.tsx` is composed
entirely of inline SVG primitives (lines, rects, circles, paths) authored
by hand. No external assets are used.

**License:** CC0 / public domain — no attribution required.

Colours are exclusively the `--art-*` design tokens defined in `globals.css`
under the `.gv-art` scope, per the Three-Style Blend design spec.
