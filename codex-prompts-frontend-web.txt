/goal You are PANE 0, lane FRONTEND-WEB. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 1, lane FRONTEND-WEB. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 2, lane FRONTEND-WEB. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 3, lane FRONTEND-WEB. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
/goal You are PANE 4, lane FRONTEND-WEB. Read docs/parallel-sessions/producer.md and codex-tasks/open.txt, then iterate per the FLAT protocol until rate-limited. Ship product PRs only (apps/ packages/ workers/ infra/); GATE-FIRST if release-validation is red.
