/goal PR-FIX-MAC. Loop until triage.jsonl has no unclaimed entries. Each cycle: read the next triage.jsonl entry in order, skip PRs already claimed by any worker:* label, claim the PR with worker:pr-fix-mac, then fix it using docs/pr-rescue/{category}.md for that entry's category. Keep the patch minimal on the PR branch, push the repaired branch, record the outcome in docs/pr-rescue/RESULTS.md when applicable, and never create tickets. Iterate.
/goal PR-FIX-BILLY-LAPTOP. Loop until triage.jsonl has no unclaimed entries. Each cycle: read the next triage.jsonl entry in order, skip PRs already claimed by any worker:* label, claim the PR with worker:pr-fix-billy-laptop, then fix it using docs/pr-rescue/{category}.md for that entry's category. Keep the patch minimal on the PR branch, push the repaired branch, record the outcome in docs/pr-rescue/RESULTS.md when applicable, and never create tickets. Iterate.
/goal PR-FIX-LAPTOP-OLD. Loop until triage.jsonl has no unclaimed entries. Each cycle: read the next triage.jsonl entry in order, skip PRs already claimed by any worker:* label, claim the PR with worker:pr-fix-laptop-old, then fix it using docs/pr-rescue/{category}.md for that entry's category. Keep the patch minimal on the PR branch, push the repaired branch, record the outcome in docs/pr-rescue/RESULTS.md when applicable, and never create tickets. Iterate.
