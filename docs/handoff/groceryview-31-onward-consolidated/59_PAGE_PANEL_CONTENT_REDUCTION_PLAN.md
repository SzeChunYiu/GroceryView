# 59 — Page Panel Content Reduction Plan

## Goal

Make pages clear by not showing everything at once.

## Home

Show:

```text
hero
search
domain cards
best deals
market snapshot
map preview
```

Hide/move:

```text
technical source details → data sources page
debug/state → admin
long methodology → methodology page
```

## Search

Show:

```text
search bar
domain chips
filters
sort
results
```

Preview:

```text
product quick view drawer
store quick view drawer
deal explanation drawer
```

Move backstage:

```text
pagination implementation
query timing
ranking debug
raw search vector
```

## Market

Show:

```text
KPI row
main chart
category table
heatmap
watchlist
```

Preview:

```text
category preview drawer
chain preview drawer
data quality drawer
```

Move backstage:

```text
index build logs
source run diagnostics
raw observation counts by run
```

## Browse

Show:

```text
category cards
chain cards
popular searches
```

Preview:

```text
category card hover/tap preview
```

Move backstage:

```text
category generation logic
source table names
```

## Product

Show:

```text
product hero
current price answer
where to buy
price history
similar products
evidence summary
```

Preview:

```text
store quote preview
deal explanation drawer
source evidence drawer
```

Move backstage:

```text
algorithm function names
raw chart series builder
parser versions
```

## Map

Show:

```text
layers
map
selected detail
list fallback
```

Preview:

```text
store bottom sheet
station bottom sheet
pharmacy bottom sheet
kommun drawer
```

Move backstage:

```text
coordinate parser logs
Overpass query debug
map clustering debug
```

## Pharmacy

Show:

```text
safety boundary
exact EAN comparisons
history
alerts
```

Move backstage:

```text
connector details
raw EAN match logs
parser version
```

## Fuel

Show:

```text
grade selector
price chart
operator table
station candidates
detour calculator
```

Move backstage:

```text
source table names
crowd moderation internals
raw station feed logs
```
