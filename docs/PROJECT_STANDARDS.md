# Project standards

## No fabricated market data

GroceryView must not fabricate price values, index values, historical time series, deltas, confidence scores, source coverage, or freshness. If a source is only documented, UI must say that ingestion is not implemented. Official CPI/HICP, regulated references, upstream agriculture, and energy context must be labeled as separate layers from GroceryView retail observations.
