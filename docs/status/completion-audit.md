# GroceryView Completion Audit

## Objective restatement

User objective: ship all GroceryView project deliverables, check all tasks, and after each iteration open a PR and merge it to `main`.

This audit maps the objective and proposal requirements to concrete artifacts in the repository. It is not a claim that the full product is complete.

## Prompt-to-artifact checklist

| Requirement / deliverable | Evidence | Status |
| --- | --- | --- |
| Iterative PR + merge workflow | Shipped iterations continue to land through short-lived PRs and squash merges to `main`; latest verified UI/API connection batch includes PR #463 for human review operations | Done for shipped iterations |
| Product proposal source | `PROPOSAL.md` | Present |
| MVP web dashboard | PR #1, `apps/web` | Foundation shipped |
| Product price terminal UI | This PR, `apps/web/scripts/pages.mjs`, `apps/web/public/styles.css`, and web page-generation tests show a TradingView-style product page with quote strip, Stockholm/local distribution histogram, percentile table, candlestick-style history, range, freshness, and evidence-volume numbers | Completed by this PR merge |
| Market mover UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests add a Seeking Alpha-style grocery mover board to `/market/` with current price, 1M move, 52W position, same-product Stockholm median gap, and verified-observation counts | Completed by this PR merge |
| Connected market mover UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire `/market/` to fetch `/api/market/overview` and render live mover leader, 1M move, 52-week range/position, Stockholm median gap, and verified-history evidence from the API response | Completed by this PR merge |
| Market movers API | This PR, `createGroceryViewApi().getMarketOverview()`, `/api/market/overview`, and API/server tests expose mover rows with current price, 1M move, 52-week range/position, Stockholm median gap, and verified-history counts for connected market boards | Completed by this PR merge |
| Connected category market UI/API | This PR, `createGroceryViewApi().getCategoryMarket()`, `/api/categories/{category}/market`, and `apps/web/scripts/pages.mjs` wire the coffee category page to live category rows with current price, Deal Score, 1M move, 52-week position, Stockholm median gap, and verified-history evidence | Completed by this PR merge |
| Current-inclusive 52-week ranges | This PR, `packages/api/src/index.ts`, and API route tests ensure product terminal and market mover 52-week ranges include the current best quote, so a live current low cannot render below the displayed 52-week low | Completed by this PR merge |
| Next app route export hardening | This PR, `apps/web/src/lib/grocery-index-widget.ts`, `apps/web/src/app/chain-index/page.tsx`, and web route tests move the embeddable Grocery Index widget contract out of a Next page export so `next build --webpack` can type-check and prerender all app routes | Completed by this PR merge |
| Deterministic web workspace build | This PR, `apps/web/package.json`, `apps/web/scripts/pages.test.mjs`, and `rtk npm run build -w @groceryview/web` make the web workspace build use the verified webpack path so local and CI workspace builds avoid the macOS Turbopack native-binding blocker while still type-checking and prerendering all 306 app routes | Completed by this PR merge |
| Product price terminal API | This PR, `createGroceryViewApi().getProductPriceTerminal()`, `/api/products/{id}/terminal`, and server/OpenAPI tests expose quote, Stockholm/local distribution, chart series, history summary, and evidence guardrails for connected clients | Completed by this PR merge |
| Connected product terminal UI | This PR, `apps/web/scripts/pages.mjs`, `apps/web/public/styles.css`, and web page-generation tests wire the coffee product page to fetch `/api/products/{id}/terminal` from the configured API base and render live quote, 1M move, 52W range, Stockholm/local distribution, verified-history evidence, and chart-series summaries without unsafe HTML injection | Completed by this PR merge |
| Connected price confidence UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the price confidence guide to `/api/products/{id}/terminal`, rendering live quote eligibility, current price-row volume, verified-history ratio, evidence guardrails, and chart confidence styling from the public API response | Completed by this PR merge |
| Nest product terminal API surface | This PR, `apps/api/src/products/products.controller.ts`, and Nest API app e2e/OpenAPI tests expose `/products/{id}/terminal` with quote, Stockholm/local distribution, chart-series, history, and evidence guardrail data for clients using the newer API app | Completed by this PR merge |
| Mobile product terminal summary | This PR, `apps/mobile/src/index.ts`, `apps/mobile/src/queryCache.ts`, and mobile tests add a selected-product terminal summary, `ProductPriceTerminalScreen` route blueprint, and offline-first `productTerminal` query key for quote, Stockholm/local distribution, history, chart, and guardrail data | Completed by this PR merge |
| Hosted product terminal smoke | This PR, `infra/scripts/smoke-hosted-http.sh`, `buildHostedSmokeCommandPlan()`, `infra/README.md`, and ops tests add a deployment smoke probe for `/api/products/${GROCERYVIEW_TERMINAL_PRODUCT_ID:-coffee}/terminal` that validates matching product id, ticker, quote, distribution, and chart JSON | Completed by this PR merge |
| Core Deal Score, basket comparison, indices | PR #1, `packages/core` tests | Foundation shipped |
| Deal Score report API | PR #316, `createGroceryViewApi().getDealScore()`, `/api/products/{id}/deal-score`, and server/OpenAPI tests expose the score, band, median discount, historical percentile, confidence, and customer-facing reasons | Shipped after merge |
| Connected daily deals UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire Today’s best grocery deals to `/api/market/overview`, rendering live top deal ticker, best price, store, Deal Score, verdict mix, and ranked-deal count from the public market API | Completed by this PR merge |
| Connected store deals UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the Willys Odenplan store page to `/api/stores/{id}/deals`, rendering live top in-store deal, current price, Deal Score, category coverage, and Buy/Compare verdict mix from the public store-deals API | Completed by this PR merge |
| Connected store map UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the Stockholm store map to `/api/stores`, rendering live mapped-store count, chains, district coverage, and high-confidence profile counts from the public stores API | Completed by this PR merge |
| Connected store comparison UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the Stockholm store comparison page to `/api/basket/compare`, rendering live favorite-store totals, best single-store option, split-basket total, split-store count, missing products, and savings against the best single-store basket from the protected API response | Completed by this PR merge |
| Connected shopping route planner UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the shopping route planner to `/api/basket/compare`, rendering live stop count, assigned products, split-basket total, split-store count, savings, and missing-product blockers from the protected API response | Completed by this PR merge |
| Product comparison details API | PR #314, `createGroceryViewApi().getProductEquivalents()`, `/api/products/{id}/equivalents`, and server/OpenAPI tests expose comparable same-category products with best known price, best store, Deal Score, and explanation | Shipped after merge |
| Price freshness report API | PR #300, `createGroceryViewApi().getPriceFreshnessReport()`, `/api/prices/freshness`, and server/OpenAPI tests expose verified-price age, stale/aging/fresh counts, and backfill product IDs | Shipped after merge |
| Connected price freshness UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the retailer freshness page to `/api/prices/freshness` and render fresh/aging/stale counts, backfill IDs, thresholds, and stale-product names from the API response | Completed by this PR merge |
| Connected catalog coverage UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the catalog coverage page to `/api/prices/freshness`, `/api/stores`, and `/api/market/overview`, rendering live product/category coverage, freshness mix, store-chain footprint, market rows, and backfill actions from public API responses | Completed by this PR merge |
| Watchlist, alerts, budget summaries | PR #2, PR #11 | Foundation shipped |
| Connected watchlist UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the watchlist workbench to `/api/watchlist`, rendering tracked-item counts, target-price rules, active trigger values, favorite-store scope, and 52-week-low alert counts from the protected API response | Completed by this PR merge |
| Connected budget forecast UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the budget forecast page to `/api/budget/summary`, rendering weekly and monthly budget status, actual remaining buffers, next-basket estimate, after-estimate buffer, and over/under statuses from the protected API response | Completed by this PR merge |
| Connected savings ledger UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the savings ledger page to protected `/api/budget/summary` and `/api/basket/compare`, rendering budget actuals, next-basket estimate, split-basket forecast savings, assignment evidence, and missing-product blockers before savings are treated as realized | Completed by this PR merge |
| Connected smart swaps UI | This PR, `apps/web/scripts/pages.mjs`, and web page-generation tests wire the smart swaps page to `/api/products/{id}/equivalents` plus the product terminal quote, rendering live comparable-product count, best swap price, Deal Score, estimated savings, and equivalence reason from public API responses | Completed by this PR merge |
| API foundation | PR #3, `packages/api` | Foundation shipped |
| API mutable route input validation | PR #132, `packages/api` route validation tests | Shipped after merge |
| SQL data schema | PR #4, `db/schema.sql` | Foundation shipped |
| PostgreSQL provenance schema | PR #111, `infra/db/SCHEMA.md`, `infra/db/migrations/001_groceryview_schema.sql` | Shipped after merge |
| Database migration verifier | PR #112, `infra/db/scripts/verify-migrations.sh` | Shipped after merge |
| HTTP server | PR #5, `packages/server` | Foundation shipped |
| SEO pages | PR #6, `apps/web/scripts/pages.mjs` | Foundation shipped |
| Persistence/migrations | PR #7, `packages/db`, `db/migrations` | Contract/in-memory shipped |
| Auth/session enforcement | PR #8, `packages/auth`, server auth tests | Foundation shipped |
| Auth provider session exchange | PR #325, `/api/auth/session`, `authSessionExchange`, server/OpenAPI tests, and web login API bridge exchange verified provider assertions for short-lived bearer sessions without storing tokens in localStorage | Completed by this PR merge |
| Mobile app shell / scan contracts | PR #9, `apps/mobile` | View-model foundation shipped |
| Generated artifact hygiene | PR #10, `.gitignore`, `gitignore.test.mjs` | Shipped |
| Notification planning | PR #11, `planNotifications` | Planning shipped |
| Ingestion normalization | PR #12, `packages/ingestion` | Foundation shipped |
| Product matching / smart swaps | PR #13, matching tests | Foundation shipped |
| Receipt review | PR #14, `reviewReceiptScan` | Structured review shipped |
| Household mode | PR #15, household tests | Foundation shipped |
| Household plan API bridge | PR #323, `createGroceryViewApi().upsertHouseholdPlan()`, `/api/households/current`, server auth/OpenAPI tests, and web household API bridge calls save member-attributed baskets, shared stores, budget summary, and approval policy through protected routes | Completed by this PR merge |
| Ad trust policy | PR #16, ad policy tests | Foundation shipped |
| Nutrition per krona / meal planning | PR #17, nutrition meal tests | Foundation shipped |
| Privacy controls | PR #18, privacy tests | Planning shipped |
| Privacy export and deletion-plan API | PR #311, `/api/privacy/export`, `/api/privacy/deletion-plan`, server auth/OpenAPI tests, and web privacy API bridge calls expose protected account export/deletion planning without destructive deletion | Completed by this PR merge |
| API contract manifest | PR #19, `buildOpenApiDocument` | Foundation shipped |
| Deployment runtime config | PR #20, deploy manifest/runtime tests | Foundation shipped |
| Server runtime entrypoint | PR #272, `createRuntimeHttpHandler()`, direct `node packages/server/dist/index.js` execution guard, and env-backed health/auth option wiring make the deploy start command executable | Shipped after merge |
| Runtime repository/sink wiring | PR #278, `RuntimePersistenceRepository`, `buildRepositoryBackedAuthOptions()`, and `createRuntimeHttpHandler(..., { repository })` connect runtime account subscription access, billing webhook persistence, notification suppression persistence, and human-review repository operations to one injected repository | Shipped by this PR merge |
| PostgreSQL runtime bootstrap | PR #281, `createRuntimeHttpService()`, `pg`, `createPgQueryExecutor()`, and `createPostgresRepository()` let the runtime server build repository-backed account, billing, notification, and human-review paths from `DATABASE_URL` | Shipped by this PR merge |
| PostgreSQL readiness endpoint | PR #283, `/api/readiness/postgres`, `postgresReadinessProvider`, and the runtime `DATABASE_URL` pool query executor expose token-protected schema/migration readiness evidence without leaking database secrets | Completed by this PR merge |
| CI verification | PR #21, `.github/workflows/ci.yml` | Shipped |
| Deploy workflow skeleton | PR #22, `.github/workflows/deploy.yml` | Provider-neutral skeleton shipped |
| Deploy environment secret audit | This PR, `.github/workflows/deploy.yml`, `scripts/ops/check-production-secrets.mjs`, `tests/schema/deploy-workflow.test.mjs`, and `tests/schema/production-secrets-script.test.mjs` make deploy preflight list production environment secrets with `gh secret list --env production` and fail closed before test/build/deploy handoff when required workflow/runtime secrets are absent | Completed by this PR merge |
| Hosted production smoke workflow | This PR, `.github/workflows/hosted-smoke.yml` and `tests/schema/hosted-smoke-workflow.test.mjs` add a production-environment manual/daily workflow that fails closed without `GROCERYVIEW_API_BASE_URL` and `METRICS_TOKEN`, runs hosted HTTP product-terminal smoke plus token-protected PostgreSQL, source-run, catalog-coverage, scan-provider, and scan-upload readiness smokes, and uploads JSON evidence artifacts | Completed by this PR merge |
| Deploy post-publish hosted smoke gate | This PR, `.github/workflows/deploy.yml`, `tests/schema/deploy-workflow.test.mjs`, and existing `infra/scripts/smoke-hosted-http.sh` / `infra/scripts/smoke-hosted-readiness.sh` make the production deploy fail unless the freshly published API/product-terminal and token-protected PostgreSQL, source-run, catalog-coverage, scan-provider, and scan-upload readiness smokes pass, with JSON smoke artifacts uploaded for deploy evidence | Completed by this PR merge |
| Production workflow variable audit | This PR, `scripts/ops/check-production-secrets.mjs`, deploy/hosted-smoke workflow env wiring, and schema tests fail closed when required production workflow variables for the web URL, product-terminal smoke product, or scanner smoke account are absent | Completed by this PR merge |
| Completion audit | PR #23, `docs/status/completion-audit.md` | Shipped |
| PostgreSQL repository adapter | PR #24, `createPostgresRepository` | Adapter skeleton shipped |
| PostgreSQL integration readiness contract | PR #123, `buildPostgresIntegrationReadinessReport` | Schema/migration/probe gate ready for live DB wiring |
| Repository governance policy | PR #25, `.github/repository-ruleset.json`; GitHub ruleset #16607866 active on `main` | Applied in GitHub |
| PostgreSQL query executor | PR #26, `createPgQueryExecutor` | Client adapter shipped |
| Static web flow scaffolds | PR #27, `apps/web/scripts/pages.mjs` | Static route scaffolds shipped |
| Web scanner review desk | PR #135, `apps/web/src/main.ts`, `apps/web/scripts/pages.mjs` | Shipped after merge |
| Web flow actions | PR #286, `apps/web/scripts/pages.mjs`, `apps/web/public/styles.css`, and `apps/web/scripts/pages.test.mjs` add tested login, account, household, privacy, basket, and scanner controls with safe client-side result updates | Completed by this PR merge |
| Web API session bridge | PR #309, `apps/web/scripts/pages.mjs`, `apps/web/public/styles.css`, and `apps/web/scripts/pages.test.mjs` add a session bridge that stores API base/user id in local storage, keeps bearer tokens in session storage, and saves account alerts plus basket lines through protected API routes when configured | Completed by this PR merge |
| Notification delivery foundation | PR #28, `packages/notifications` | Provider-neutral delivery shipped |
| Notification provider readiness gates | PR #136, `buildNotificationProviderReadinessReport` | Shipped after merge |
| Notification SendGrid/Expo provider adapters | This PR, `packages/notifications/src/index.ts`, `packages/notifications/src/__tests__/delivery.test.ts`, `scripts/ops/check-production-secrets.mjs`, `scripts/ops/validate-production-env.mjs`, `.env.example`, and `deploy/groceryview.manifest.json` add real SendGrid email and Expo push delivery adapters with fail-closed provider errors and require their production credentials in readiness/secret gates | Completed by this PR merge |
| Runtime notification provider worker wiring | This PR, `packages/server/src/index.ts` and `packages/server/src/__tests__/runtimeConfig.test.ts` load SendGrid/Expo credentials from runtime env, instantiate the real provider adapters, and run repository-backed notification tasks through `/api/workers/notifications/run` with delivered task acknowledgements persisted | Completed by this PR merge |
| Notification worker orchestration | PR #90, `runNotificationWorkerTick` | Shipped after merge |
| Notification task persistence | PR #100, `notification_tasks`, repository methods | Shipped after merge |
| Notification suppression filtering | PR #101, `applyNotificationSuppressions` | Shipped after merge |
| Notification suppression persistence | PR #102, `notification_suppressions`, repository methods | Shipped after merge |
| Notification suppression event processing | PR #103, `processNotificationSuppressionEvent` | Shipped after merge |
| Notification suppression webhook route | PR #104, `/api/notifications/suppression-events` signature gate and persistence sink | Shipped after merge |
| Notification worker suppression enforcement | PR #105, `runNotificationWorkerTick` suppressions input and suppressed acknowledgements | Shipped after merge |
| Notification task acknowledgement persistence | PR #106, `applyNotificationTaskAcknowledgements` and `suppressed` task state | Shipped after merge |
| Notification delivery observability | PR #107, `buildNotificationOperationsReport` health/blocker metrics | Shipped after merge |
| Notification metrics export | PR #108, `formatNotificationOperationsMetrics` Prometheus-style metrics | Shipped after merge |
| Notification metrics endpoint | PR #109, `/api/metrics/notifications` metrics-token protected export | Shipped after merge |
| Production notification metrics scrape | This PR, `.github/workflows/notification-metrics-scrape.yml` and `tests/schema/notification-metrics-scrape-workflow.test.mjs` add a scheduled/manual production metrics scrape that fails closed without `GROCERYVIEW_API_BASE_URL` and `METRICS_TOKEN`, requires notification worker Prometheus series, and uploads the scrape artifact as operations evidence | Completed by this PR merge |
| Notification operations alert routing | PR #110, `planNotificationOperationsAlerts` for blocked reports | Shipped after merge |
| Notification worker cron trigger | This PR, `.github/workflows/notification-worker.yml`, and `tests/schema/notification-worker-workflow.test.mjs` schedule the protected `/api/workers/notifications/run` endpoint, fail closed on missing production URL/token secrets, and fail the run when worker health is not healthy or delivery failures remain | Completed by this PR merge |
| Workflow production secret audit coverage | This PR, `scripts/ops/check-production-secrets.mjs` and `tests/schema/production-secrets-script.test.mjs` add `GROCERYVIEW_API_BASE_URL` and `EXPO_TOKEN` to the fail-closed GitHub Actions secret audit so notification-worker and EAS build workflows cannot be considered ready while their required secrets are absent | Completed by this PR merge |
| Repository-backed notification worker cycle | PR #127, `runRepositoryNotificationWorkerCycle` repository orchestration | Shipped after merge |
| Human review queue planning | PR #29, `planHumanReviewQueue` | Core queue planning shipped |
| Human review decision application | PR #91, `applyHumanReviewDecision` | Shipped after merge |
| Human review assignment planning | PR #92, `planHumanReviewAssignments` | Shipped after merge |
| Human review SLA summary | PR #93, `summarizeHumanReviewSla` | Shipped after merge |
| Community report abuse controls | PR #94, `planCommunityReportAbuseControls` | Shipped after merge |
| Human review assignment persistence | PR #95, `human_review_assignments`, repository methods | Shipped after merge |
| Human review permission checks | PR #96, `authorizeHumanReviewAction` | Shipped after merge |
| Human reviewer role persistence | PR #97, `human_reviewers`, repository methods | Shipped after merge |
| Community reporter trust persistence | PR #98, `community_reporter_trust`, repository methods | Shipped after merge |
| Human review SLA alert planning | PR #99, `planHumanReviewSlaNotifications` | Shipped after merge |
| Connected human review operations UI | PR #463, `apps/web/scripts/pages.mjs`, and web page-generation tests wire `admin/human-review/index.html` to protected `/api/human-review/assignments`, rendering live open/in-progress assignment counts, SLA overdue/due-soon status, high-priority rows, and reviewer-session failure messaging | Shipped after merge |
| Reviewer decision UI actions | This PR, `apps/web/src/components/price-report-review-actions.tsx`, and web route tests add signed-in reviewer approve, reject, and `needs_more_info` controls that POST to `/api/human-review/assignments/{id}/decisions` with the session bearer token and keep anonymous moderation blocked | Completed by this PR merge |
| Scanning pipeline foundation | PR #30, `packages/scanning` | Provider-neutral scan pipeline shipped |
| Scan processing API bridge | PR #318, `/api/scans/process`, `scanProviders`, server auth/OpenAPI tests, and web scanner API bridge calls process barcode/receipt payloads through configured providers and return human-review work items | Completed by this PR merge |
| Scan upload storage bridge | PR #329, `prepareScanUploadTicket()`, `/api/scans/upload-url`, `scanUploadStorage`, server auth/OpenAPI tests, and web scanner upload-ticket bridge create private upload tickets before scan processing while failing closed without configured storage | Completed by this PR merge |
| Scanner browser upload transfer | PR #333, `apps/web/scripts/pages.mjs`, and web page-generation tests perform a provider-neutral `PUT` to `ticket.uploadUrl` with returned ticket headers and the selected file payload before scan processing | Completed by this PR merge |
| Runtime S3-compatible scan upload storage | PR #338, `loadRuntimeConfig()`, `buildRuntimeAuthOptions()`, `.env.example`, and server/runtime tests wire `S3_*` plus `SCAN_UPLOAD_MAX_BYTES` into signed scanner upload tickets without leaking storage secrets | Completed by this PR merge |
| Scan upload storage readiness endpoint | This PR, `/api/readiness/scan-upload-storage`, `buildScanUploadStorageReadinessReport()`, S3-backed runtime upload-ticket signing, OpenAPI docs, hosted readiness smoke coverage, and production secret/env validators make object-storage readiness observable without leaking S3 secrets | Completed by this PR merge |
| Scan upload CORS readiness endpoint | This PR, `/api/readiness/scan-upload-cors`, `buildScanUploadCorsReadinessReport()`, runtime CORS preflight probing, OpenAPI docs, and hosted readiness smoke coverage make browser upload CORS readiness observable without exposing signed upload URL secrets | Completed by this PR merge |
| Scan upload write readiness endpoint | This PR, `/api/readiness/scan-upload-write`, `buildScanUploadWriteReadinessReport()`, runtime signed-ticket PUT probing, OpenAPI docs, and hosted readiness smoke coverage make actual object write readiness observable without exposing signed upload URL secrets | Completed by this PR merge |
| Hosted scanner upload smoke | This PR, `infra/scripts/smoke-hosted-scanner-upload.mjs`, hosted/deploy smoke workflow wiring, and ops/schema tests add account-bound `/api/scans/upload-url` plus signed `PUT` evidence without recording bearer tokens or signed URLs | Completed by this PR merge |
| Completion audit refresh | PR #31, `docs/status/completion-audit.md` | Audit reconciled after scanning |
| Mobile Expo readiness | PR #32, `apps/mobile/app.config.json`, `apps/mobile/eas.json` | Device-build metadata shipped |
| Mobile Expo Router screen shell | This PR, `apps/mobile/app/**/*.tsx`, `apps/mobile/src/native/GroceryViewNativeScreen.tsx`, `apps/mobile/package.json`, and `tests/schema/mobile-expo-router-screens.test.mjs` add Expo Router entry files for every authenticated MVP route, declare Expo/React Native runtime dependencies, and render the tested mobile blueprint/actions through React Native primitives | Completed by this PR merge |
| Mobile native camera capture screens | This PR, `apps/mobile/src/native/MobileScanCaptureScreen.tsx`, scan route files, `apps/mobile/package.json`, `apps/mobile/tsconfig.*.json`, and `tests/schema/mobile-camera-capture-screens.test.mjs` add Expo Camera-backed barcode scanning and receipt image capture entrypoints with typed TSX build coverage | Completed by this PR merge |
| Runtime OCR.space receipt provider | This PR, `packages/scanning/src/index.ts`, `packages/server/src/index.ts`, `packages/scanning/src/__tests__/pipeline.test.ts`, and `packages/server/src/__tests__/runtimeConfig.test.ts` add a concrete OCR.space receipt OCR adapter, fail-closed `OCR_SPACE_API_KEY` runtime config, and scan-provider wiring for `/api/scans/process` | Completed by this PR merge |
| Runtime OpenFoodFacts barcode provider | This PR, `packages/scanning/src/index.ts`, `packages/server/src/index.ts`, `.env.example`, `scripts/ops/check-production-secrets.mjs`, `scripts/ops/validate-production-env.mjs`, `packages/scanning/src/__tests__/pipeline.test.ts`, and `packages/server/src/__tests__/runtimeConfig.test.ts` add a concrete OpenFoodFacts barcode lookup adapter, required user-agent runtime config, and scan-provider wiring for `/api/scans/process` | Completed by this PR merge |
| Scan provider readiness endpoint | This PR, `/api/readiness/scanning`, `buildRuntimeScanProviderReadinessReport()`, OpenAPI docs, `infra/scripts/smoke-hosted-readiness.sh`, server readiness tests, and hosted-smoke workflow tests add token-protected scan-provider readiness evidence and make hosted readiness smoke fail closed until barcode/OCR health proof is available | Completed by this PR merge |
| Live scan provider health checks | This PR, `buildRuntimeScanProviderReadinessReport()`, `OCR_SPACE_HEALTHCHECK_IMAGE_URL`, `OPENFOODFACTS_HEALTHCHECK_BARCODE`, `.env.example`, production env/secret validators, and runtime config tests make `/api/readiness/scanning` execute real barcode and receipt provider probes before returning ready | Completed by this PR merge |
| Mobile EAS device-build workflow | This PR, `.github/workflows/mobile-device-build.yml`, `apps/mobile/app.config.json`, `apps/mobile/eas.json`, and `tests/schema/mobile-device-build-workflow.test.mjs` add a production-environment manual EAS build trigger that verifies the mobile package and fails closed without `EXPO_TOKEN` before starting iOS/Android store builds | Completed by this PR merge |
| Account subscription UI scaffold | PR #256, account page surfaces subscription access policy state and `/api/account/subscription-access` provenance | Shipped after merge |
| Monetization foundation | PR #33, `packages/monetization` | Provider-neutral ad/billing contracts shipped |
| Monetization provider readiness gates | PR #143, `buildMonetizationProviderReadinessReport` | Shipped after merge |
| Subscription entitlement persistence | PR #231, `subscription_entitlements`, repository methods, migrations, and PostgreSQL readiness probes | Shipped after merge |
| Subscription access policy | PR #234, `buildSubscriptionAccessPolicy` maps entitlements to premium/free enforcement decisions | Shipped after merge |
| Subscription access account API | PR #244, `createGroceryViewApi().getSubscriptionAccess()` and `/api/account/subscription-access` expose entitlement policy to account clients | Shipped after merge |
| Repository-backed subscription access | PR #251, `/api/account/subscription-access` can load persisted entitlement rows via a configured repository before falling back to in-memory API state | Shipped after merge |
| Billing subscription webhook contract | PR #264, `/api/billing/subscription-events` verifies `x-groceryview-billing-signature`, rejects sensitive payment fields, normalizes subscription events, and persists entitlement mutations through a configured sink | Shipped after merge |
| Stripe billing webhook signature adapter | This PR, `/api/billing/subscription-events` accepts provider-native `stripe-signature` `t=`/`v1=` HMAC headers with a 300-second replay tolerance while preserving `x-groceryview-billing-signature` compatibility | Completed by this PR merge |
| Stripe checkout session bridge | This PR, `/api/billing/checkout-sessions`, `STRIPE_SECRET_KEY`, and `STRIPE_PRICE_PREMIUM_*` create account-bound provider checkout sessions for signed-in users while failing closed without provider credentials or plan price ids | Completed by this PR merge |
| Stripe billing portal session bridge | This PR, `/api/billing/portal-sessions` creates account-bound Stripe-compatible billing portal sessions for signed-in users with persisted provider-customer evidence and fails closed without that evidence | Completed by this PR merge |
| Account billing action controls | This PR, `apps/web/src/components/account-billing-actions.tsx`, `apps/web/src/app/account/page.tsx`, and web route tests add signed-in subscription status, Stripe checkout, and billing portal controls that use `sessionStorage` bearer tokens and fail closed with no anonymous billing sessions | Completed by this PR merge |
| Retailer compliance gate | PR #34, `planRetailerSourceAccess` | Source-access gate shipped |
| Retailer connector runner | This PR, `runRetailerConnector()`, `fetchRetailerConnectorSnapshot()`, and ingestion tests execute approved connector plans, hash fetched snapshots, stamp provenance, ingest parsed products, and fail closed before unsafe pulls | Completed by this PR merge |
| Retailer connector pull smoke | This PR, `infra/scripts/smoke-retailer-connector.sh`, `infra/README.md`, and ops tests provide a source-gated smoke command that pulls a configured endpoint and prints content hash/raw snapshot evidence | Completed by this PR merge |
| Retailer normalized JSON parser | This PR, `parseRetailerProductJsonSnapshot()`, and ingestion tests convert approved connector JSON snapshots into ingestible product rows while failing closed on malformed payloads | Completed by this PR merge |
| Open Prices real-data pull | This PR, `buildOpenPricesConnectorUrl()`, `parseOpenPricesSnapshot()`, `infra/scripts/smoke-open-prices.sh`, `infra/README.md`, and ingestion/ops tests pull public Open Food Facts Open Prices rows for SEK/Sweden, normalize usable rows into GroceryView price observations, optionally write `acceptedObservations` to `OPEN_PRICES_OUTPUT_PATH`, and fail closed without accepted real price rows | Completed by this PR merge |
| Open Prices PostgreSQL import path | This PR, `persistOpenPricesArtifact()`, `infra/scripts/import-open-prices-artifact.sh`, `infra/db/migrations/006_source_runs_official_api.sql`, and db/ops/schema tests import a saved Open Prices artifact into `source_runs`, `raw_records`, `products`, `aliases`, immutable `observations`, and `latest_prices` through `DATABASE_URL` | Completed by this PR merge |
| Bounded bulk daily ingestion runner | This PR, `buildDailyConnectorConfigsFromEnv()`, `runDailyIngestionFromEnv()`, `.github/workflows/daily-ingestion.yml`, ingestion tests, and the production runbook add bounded connector count/concurrency/start-delay/retry controls while still validating all six required chains before any canary cap and always attempt to upload `groceryview-production-ingestion-config` evidence for generated connectors, catalog targets, and production-env validation on success or validation-failure paths | Completed by this PR merge |
| Daily live store enumeration evidence | This PR, `.github/workflows/daily-ingestion.yml`, `ops:daily-connector-stores`, schema tests, and the production runbook export live required-chain branch metadata before connector/target validation, fail closed on missing or empty chain store arrays, and always attempt to upload `groceryview-daily-connector-stores` as workflow evidence for success or failure diagnostics | Completed by this PR merge |
| Daily connector-to-observations chain summaries | This PR, `runDailyIngestion()`, `.github/workflows/daily-ingestion.yml`, ingestion tests, schema tests, runtime source-run readiness, and the production runbook add per-connector `chainSummaries`, require every required chain to succeed with persisted observation ids, fail official product connectors before persistence when configured branch stores emit no observations, mark already-created source runs failed when persistence aborts, require configurable per-chain minimum accepted-row thresholds for `/api/readiness/source-runs`, make deployed source-run readiness fail on nonzero blockers, missing fresh chains, insufficient accepted-row blockers, fewer than six succeeded evidence entries, or no latest successful finish timestamp, always attempt to upload `groceryview-daily-ingestion-result` as success/failure workflow evidence, and preserve deployed `postgres-readiness.json`, `source-run-readiness.json`, and `catalog-coverage-readiness.json` in the `groceryview-deployed-readiness` artifact | Completed by this PR merge |
| DB-to-site latest-price snapshot export | This PR, `createPostgresSiteSnapshotReader()`, `scripts/ingestion/export-db-site-snapshot.mjs`, `.github/workflows/daily-ingestion.yml`, and schema/runbook tests export public static-site rows from `postgres.latest_prices` with product, chain, store, category, freshness, coverage, and provenance fields, fail closed when no latest-price rows exist, require all launch chains, generated target-store external refs, generated target products, generated target categories, required store price-type pairs, and fresh observations to have latest-price evidence, check deployed `/api/readiness/catalog-coverage` for zero missing chain/store/product/category/price-type dimensions plus zero product-store and store-price-type gaps, and upload the daily workflow `groceryview-db-site-snapshot` artifact containing both normalized snapshot rows and `db-site-snapshot-result.json` status/coverage summary after ingestion succeeds | Completed by this PR merge |
| Open Prices data-pipeline visibility | This PR, `open_prices_real_pull_plan`, `build_open_prices_pull_plan()`, Dagster smoke expected assets, and worker tests expose the Open Prices endpoint, parser, required `OPEN_PRICES_USER_AGENT`, smoke command, and evidence fields in the data-worker lane | Completed by this PR merge |
| Deployment ops foundation | PR #35, `packages/ops` | Readiness/rollback gates shipped |
| Scheduled background worker deployment gates | PR #134, `buildDeploymentReadinessReport` scheduled job checks | Shipped after merge |
| Catalog coverage reporting | PR #36, `packages/catalog` | Coverage/backfill accounting shipped |
| Release validation workflow repair | PR #88, `.github/workflows/release-validation.yml`, `tests/schema/release-workflow.test.mjs` | Shipped |
| Main branch ruleset application | PR #89, GitHub ruleset #16607866, `.github/repository-ruleset.json` | Applied in GitHub |

## Verification gates

Current root verification commands:

```bash
npm test
npm run build
npm run typecheck
```

These commands cover unit/domain tests, schema/manifest tests, build output, and TypeScript checks. The current static web page connectivity audit also reports 31 generated pages with no unconnected page bodies. These gates do not prove live integrations, production deployment, or empirical data coverage.

## Not complete

The full GroceryView proposal is not complete. Current shipped work is a broad tested foundation, not a production-ready product.

## Remaining blocking gaps

- PostgreSQL query-executor wiring, repository adapter skeleton, human-review assignment persistence, a schema/migration/probe readiness contract, runtime pool bootstrap, and a token-protected PostgreSQL readiness endpoint exist; live database integration tests still need a provisioned PostgreSQL service, migrations applied, and an observed hosted smoke call against the readiness endpoint.
- Real retailer/API/crawler connectors and legal/robots review. Retailer source access gates, an approved-connector runner, a source-gated connector smoke, normalized JSON parsing, a live-proven Open Prices public API pull/normalizer with optional local `acceptedObservations` artifact output, a PostgreSQL artifact import path, a DB-to-site latest-price snapshot exporter, and data-pipeline visibility for the Open Prices pull plan exist; retailer-specific adapters, live credentials/endpoints, hosted import proof against a provisioned database, completed legal reviews, and hosted scheduled-worker proof and observed full uncapped daily-ingestion success are still missing. The Open Prices pull proves GroceryView can fetch and normalize real public price rows, but it does not replace approved retailer-specific Stockholm data.
- Real OCR/camera/upload pipeline for barcode and receipt scanning. Provider-neutral scan pipeline, protected scan-processing API bridge, protected upload-ticket bridge, browser-to-upload-url transfer code, Expo Camera barcode scanning, native receipt image capture entrypoints, token-protected scan upload storage readiness, token-protected scan upload CORS preflight readiness, and token-protected signed upload write readiness exist; real object-storage provider credentials, passing hosted `/api/readiness/scan-upload-storage`, `/api/readiness/scan-upload-cors`, and `/api/readiness/scan-upload-write` proof, live OCR provider credentials populated with passing hosted `/api/readiness/scanning` evidence, observed passing hosted scanner upload smoke run, browser/device camera upload proof, and device capture evidence still missing.
- Real Expo/React Native screens and device builds. Expo route/readiness config, Expo Router screen files, a shared React Native screen shell, native scan capture screens, and a fail-closed EAS production build workflow exist; full native feature-specific components, configured Expo credentials, and observed successful store build artifacts are still missing.
- Real interactive web UI for login, account, household, privacy, basket, scanner, notification inbox, ad disclosure, receipt review, loyalty offers, pantry replenishment, meal planning, nutrition value, and human-review flows. Static page scaffolds, connected page-level API bridges, an account subscription-access panel, signed-in billing checkout/portal controls, a scanner review desk, provider-safe client-side flow actions, a provider-neutral auth session exchange route, an API session bridge for account alert plus basket writes, protected privacy export/deletion-plan routes, scanner API processing plus upload-ticket/upload-transfer/runtime-signing bridges, household plan API bridge, and protected human-review assignment loading exist; real auth provider credentials, durable database-backed household/UI state, observed hosted scanner upload smoke proof, browser/device upload proof, and provider-backed upload/session flows remain incomplete.
- Push/email provider adapters and production notification workers. Provider-neutral delivery, worker tick orchestration, persisted task schedules, acknowledgement application, suppression filtering, persisted suppression records, suppression event normalization, a signed suppression webhook route, worker-level suppression enforcement, delivery health reporting, Prometheus-style metric export, a token-protected metrics endpoint, blocked-report alert planning, and repository-backed worker cycle orchestration and provider-readiness gates exist; production credentials, an observed passing metrics scrape, live scheduled-run evidence, and observed SendGrid/Expo delivery proof still missing.
- AdMob/AdSense and subscription billing integration. Provider-neutral monetization contracts, provider-readiness gates, subscription entitlement persistence, entitlement access policy, an account subscription-access API, repository-backed entitlement lookup, a signed billing subscription-event webhook contract, a Stripe-native webhook signature adapter, Stripe checkout-session and billing-portal bridges, runtime repository/sink injection hooks, and PostgreSQL-backed runtime repository bootstrap exist; real provider credentials in production, additional provider-specific webhook adapters, live checkout/ad-serving proof, a migrated hosted database, live billing-provider webhook proof, and interactive account UI enforcement still missing.
- Hosting provider selection, real deployment, secrets, DNS, observability, smoke tests, and rollback. Deployment readiness/rollback gates, scheduled-worker checks, a server runtime entrypoint for the manifest start command, runtime repository/sink wiring hooks, PostgreSQL pool bootstrap from `DATABASE_URL`, and a token-protected PostgreSQL readiness endpoint exist; real provider deployment, production environment secrets and workflow variables populated in GitHub, a provisioned migrated PostgreSQL service, and a successful deploy-time hosted smoke run against provisioned production infrastructure still missing.
- Full catalog/data coverage beyond seed products/stores. Catalog coverage reporting exists; real retailer/feed backfill data still missing.
- Human review queue operations for low-confidence product matching and community reports. Core queue planning, auditable decision writeback primitives, reviewer assignment/SLA planning, SLA summary primitives, community reporter abuse-control planning, assignment persistence, reviewer permission checks, persisted reviewer roles, reporter trust-state persistence, provider-neutral SLA alert planning, persisted notification task schedules, a protected admin queue API bridge, and signed-in reviewer approve/reject/needs-more-info controls exist; real provider credentials, production session-to-reviewer mapping, hosted account-enforcement proof, and live PostgreSQL proof still missing.
